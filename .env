# Security
DJANGO_SECRET_KEY=+s4l@n9x0p0e@gs2#gumt%o!xrh^)q5q-d9h2ie62ba&#-#@@d

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1  # Add this for task results

# Allowed Hosts (Required for production)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com,www.your-domain.com  # Add your domain here
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com,www.your-domain.com

# CSRF Trusted Origins (Required for HTTPS)
CSRF_TRUSTED_ORIGINS=https://your-domain.com,https://www.your-domain.com  # Add your domain here

# Static Files (Optional)
STATIC_URL=/static/
STATIC_ROOT=/code/static  # Ensure this matches your Docker volume

# Connection URLs
DATABASE_URL=postgres://x_tracker_user:x_tracker_password@db:5432/x_tracker
REDIS_URL=redis://redis:6379/0

# Database settings
POSTGRES_DB=x_tracker
POSTGRES_USER=x_tracker
POSTGRES_PASSWORD=x_tracker
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Email settings (fill these out for production)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=user@example.com
EMAIL_HOST_PASSWORD=password
# Debugging
DEBUG=True  # For development only, set to False in production
DJANGO_DEBUG=1  # Use 1 for True, 0 for False

# Security
DJANGO_SECRET_KEY=_zak0!45@o#+_#kay(vm*d2ysmlcvi+=n)e4l2k+u^(rr@ndu!

# Database
DATABASE_URL=postgres://x_tracker:x_tracker@db:5432/x_tracker
DATABASE_NAME=x_tracker
DATABASE_USER=x_tracker
DATABASE_PASSWORD=x_tracker
DATABASE_HOST=db
DATABASE_PORT=5432

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1  # Add this for task results

# Allowed Hosts (Required for production)
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com  # Add your domain here

# CSRF Trusted Origins (Required for HTTPS)
CSRF_TRUSTED_ORIGINS=https://your-domain.com  # Add your domain here

# Static Files (Optional)
STATIC_URL=/static/
STATIC_ROOT=/code/static  # Ensure this matches your Docker volume

# .env
DJANGO_SETTINGS_MODULE=config.settings.development  # or .production
# DJANGO_SETTINGS_MODULE=config.settings.development python manage.py runserver
# DJANGO_SETTINGS_MODULE=config.settings.production python manage.py runserver

# Development-specific
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Production-specific (example)
# ALLOWED_HOSTS=your-domain.com,www.your-domain.com
# CSRF_TRUSTED_ORIGINS=https://your-domain.com